extends layout 

block content
    main 
        div.header
            h1 Delete Instance
            p #{piece.name} - #{instance_id}
            hr  
        div.delete-container 
                h2 Are you sure you want to delete this instance? This action cannot be undone. 
                form(action='' method='POST') 
                    button.main-button(type='submit') Delete instance
        

 
